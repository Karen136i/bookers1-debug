<!--edit と index のテンプレート化-->
<%= form_with model:book, local:true do |f| %>

<!--バリデーション-->
  <% if book.errors.any? %>
    <div>
      <h2><%= @book.errors.count %> errors prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<% end %>
